Show the user their full ideas bank.

## What to do

1. Read `store.md`.
2. Print every stored idea EXACTLY as it appears in the file. Verbatim. Do NOT summarize, shorten, refine, or rephrase. Do NOT collapse pointers into prose. The raw thought is sacred. Show it word for word, including its original structure and line breaks.
3. Keep the day headings and idea headings (number + time) as they are.
4. After the list:
   > "which idea do you want to develop? type `/post <number>` to draft it, or `/store <new thought>` to add another."

If the bank is empty:
   > "nothing stored yet. drop your first idea with `/store <your thought>`."

## critical rule

never edit, condense, or "clean up" the thoughts when showing them. your job here is to display, not to write. refining only happens in `/post`.
